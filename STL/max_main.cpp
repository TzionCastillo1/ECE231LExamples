#include <iostream>

template <typename T>
T max(const T& a,const T& b){
	//TODO Create logic for return
	return (
}

template <typename T>
T min(const T& a, const T& b){
	//TODO Create logic for return
	return ()
}
